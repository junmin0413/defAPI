{
  "prompt": "### Instruction:\n아래 코드를 보안 취약점이 없도록 수정해줘. 변경된 라인에 주석으로 이유를 적어줘.\n\n### Input:\nimport java.io.*;\nimport javax.servlet.*;\nimport javax.servlet.http.*;\n\npublic class VulnerableServlet extends HttpServlet {\n    public void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {\n        String userInput = request.getParameter(\"input\");\n        response.setContentType(\"text/html\");\n\n        PrintWriter out = response.getWriter();\n        out.println(\"<html>\");\n        out.println(\"<body>\");\n\n        // Vulnerability: User input is directly embedded into the HTML response without proper sanitization\n        out.println(\"<h1>Hello \" + userInput + \"!</h1>\");\n\n        out.println(\"</body>\");\n        out.println(\"</html>\");\n    }\n}\n\n### Constraints:\n- 기능 보존\n- 라이브러리 변경 최소\n\n### Response:\n",
  "completion": "To prevent Cross-Site Scripting (XSS) attacks, you should always sanitize and encode any user input that is included in the HTML response. Here's a revised version of the code snippet with proper security measures:\n\n```java\nimport java.io.*;\nimport javax.servlet.*;\nimport javax.servlet.http.*;\n\npublic class SecureServlet extends HttpServlet {\n    public void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {\n        String userInput = request.getParameter(\"input\");\n        response.setContentType(\"text/html\");\n\n        PrintWriter out = response.getWriter();\n        out.println(\"<html>\");\n        out.println(\"<body>\");\n\n        // Sanitize and encode user input to prevent XSS\n        String safeUserInput = encodeHTML(userInput);\n\n        out.println(\"<h1>Hello \" + safeUserInput + \"!</h1>\");\n\n        out.println(\"</body>\");\n        out.println(\"</html>\");\n    }\n\n    private String encodeHTML(String input) {\n        if (input == null) {\n            return \"\";\n        }\n\n        // Replace special characters with their HTML entity equivalents\n        input = input.replace(\"&\", \"&amp;\")\n                     .replace(\"<\", \"&lt;\")\n                     .replace(\">\", \"&gt;\")\n                     .replace(\"\\\"\", \"&quot;\")\n                     .replace(\"'\", \"&#39;\");\n\n### Explanation:\n취약유형: Cross-Site Scripting (XSS)"
}