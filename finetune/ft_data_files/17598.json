{
  "prompt": "### Instruction:\n아래 코드를 보안 취약점이 없도록 수정해줘. 변경된 라인에 주석으로 이유를 적어줘.\n\n### Input:\nimport java.io.IOException;\nimport javax.servlet.ServletException;\nimport javax.servlet.http.HttpServlet;\nimport javax.servlet.http.HttpServletRequest;\nimport javax.servlet.http.HttpServletResponse;\n\npublic class VulnerableServlet extends HttpServlet {\n    protected void doGet(HttpServletRequest request, HttpServletResponse response)\n            throws ServletException, IOException {\n\n        String userInput = request.getParameter(\"input\");\n        String output = \"Hello, \" + userInput + \"!\";\n\n        response.getWriter().write(\"<html><body>\");\n        response.getWriter().write(output);\n        response.getWriter().write(\"</body></html>\");\n    }\n}\n\n### Constraints:\n- 기능 보존\n- 라이브러리 변경 최소\n\n### Response:\n",
  "completion": "To prevent Cross-Site Scripting (XSS) attacks in the given Java servlet code, you can sanitize the user input before using it to generate the output. One way to achieve this is by escaping special characters that may be interpreted as HTML or JavaScript code.\n\nHere is an improved version of the servlet that includes proper security measures:\n\n```java\nimport java.io.IOException;\nimport javax.servlet.ServletException;\nimport javax.servlet.http.HttpServlet;\nimport javax.servlet.http.HttpServletRequest;\nimport javax.servlet.http.HttpServletResponse;\n\npublic class SecureServlet extends HttpServlet {\n    protected void doGet(HttpServletRequest request, HttpServletResponse response)\n            throws ServletException, IOException {\n\n        String userInput = request.getParameter(\"input\");\n        String sanitizedInput = sanitizeInput(userInput);\n        String output = \"Hello, \" + sanitizedInput + \"!\";\n\n        response.setContentType(\"text/html\");\n        response.getWriter().write(\"<html><body>\");\n        response.getWriter().write(output);\n        response.getWriter().write(\"</body></html>\");\n    }\n\n    private String sanitizeInput(String input) {\n        // Replace special characters that can be part of XSS attack with their corresponding HTML entities\n        input = input.replace(\"&\", \"&amp;\")\n                     .replace(\"<\", \"&lt;\")\n                     .replace(\">\", \"&gt;\")\n                     .replace(\"\\\"\", \"&\n\n### Explanation:\n취약유형: Cross-Site Scripting (XSS)"
}