{
  "prompt": "### Instruction:\n아래 코드를 보안 취약점이 없도록 수정해줘. 변경된 라인에 주석으로 이유를 적어줘.\n\n### Input:\nimport java.sql.*;\n\npublic class SQLInjectionVulnerability {\n\n    public static void main(String[] args) {\n        try {\n            // Simulating user input coming from a form or any external source\n            String userInput = \"'; DROP TABLE users; --\";\n\n            // Constructing a SQL query using user input directly (unsafe)\n            String query = \"SELECT * FROM users WHERE username = '\" + userInput + \"'\";\n\n            // Establishing a connection to the database (Assuming database is already set up with 'users' table)\n            Connection conn = DriverManager.getConnection(\"jdbc:mysql://localhost:3306/mydatabase\", \"username\", \"password\");\n\n            // Executing the SQL query\n            Statement stmt = conn.createStatement();\n            ResultSet rs = stmt.executeQuery(query);\n\n            // Processing the result set (omitted for brevity)\n        } catch (SQLException e) {\n            e.printStackTrace();\n        }\n    }\n}\n\n### Constraints:\n- 기능 보존\n- 라이브러리 변경 최소\n\n### Response:\n",
  "completion": "import java.sql.*;\n\npublic class SecureSQLQuery {\n\n    public static void main(String[] args) {\n        try {\n            // Simulating user input coming from a form or any external source\n            String userInput = \"'; DROP TABLE users; --\";\n\n            // Establishing a connection to the database (Assuming database is already set up with 'users' table)\n            Connection conn = DriverManager.getConnection(\"jdbc:mysql://localhost:3306/mydatabase\", \"username\", \"password\");\n\n            // Using PreparedStatement with parameterized query\n            String query = \"SELECT * FROM users WHERE username = ?\";\n            PreparedStatement pstmt = conn.prepareStatement(query);\n            pstmt.setString(1, userInput);\n\n            // Executing the SQL query\n            ResultSet rs = pstmt.executeQuery();\n\n            // Processing the result set (omitted for brevity)\n        } catch (SQLException e) {\n            e.printStackTrace();\n        }\n    }\n}\n\n### Explanation:\n취약유형: SQL Injection"
}