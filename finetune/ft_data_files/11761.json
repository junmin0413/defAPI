{
  "prompt": "### Instruction:\n아래 코드를 보안 취약점이 없도록 수정해줘. 변경된 라인에 주석으로 이유를 적어줘.\n\n### Input:\nString userInput = \"admin'; DROP TABLE users; --\";\nString query = \"SELECT * FROM users WHERE username = '\" + userInput + \"'\";\n\n// Execute the query\nStatement statement = connection.createStatement();\nResultSet resultSet = statement.executeQuery(query);\n\n### Constraints:\n- 기능 보존\n- 라이브러리 변경 최소\n\n### Response:\n",
  "completion": "// User input\nString userInput = \"admin'; DROP TABLE users; --\";\n\n// SQL query with a placeholder for user input\nString query = \"SELECT * FROM users WHERE username = ?\";\n\ntry {\n    // Create a PreparedStatement with the query\n    PreparedStatement preparedStatement = connection.prepareStatement(query);\n\n    // Set the user input as a parameter to the PreparedStatement\n    preparedStatement.setString(1, userInput);\n\n    // Execute the query using PreparedStatement\n    ResultSet resultSet = preparedStatement.executeQuery();\n\n    // Process the ResultSet\n\n} catch (SQLException e) {\n    // Handle any exceptions here\n    e.printStackTrace();\n}\n\n### Explanation:\n취약유형: SQL Injection"
}